The file should:
1. Import extract_transactions
   from Project 1 pipeline
   OR reconnect directly to database

2. Calculate per product per warehouse:
   → AVG(units_sold) = avg daily demand
   → STDDEV(units_sold) = demand variability
   → MAX(units_sold) = peak demand

3. Print results